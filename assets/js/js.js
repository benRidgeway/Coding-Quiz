var startButton = document.querySelector("#start-button");

var buttonClickHandler = function(click) {
    if (click) {
      start();
    }
};

var start = function() {
//start timer
//move to first question
};
//timeLeft = 30       amount of seconds
//var time = setInterval(startTimer, 1000)




  startButton.addEventListener("click", buttonClickHandler);